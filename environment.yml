name: bbpm
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pytorch>=1.12.0
  - numpy>=1.21.0
  - matplotlib>=3.5.0
  - pyyaml>=6.0
  - tqdm>=4.64.0
  - pytest>=7.0.0
  - pip
  - pip:
    - transformers>=4.20.0  # Optional for LLM experiments
