# Experiment 1: Capacity Scaling
# Replicates "cos sim fidelity vs N" trend

D: 1000000  # Total memory slots
d: 64       # Vector dimension
K: 50       # Active slots per item
H: 1        # Number of hashes

# Item counts to test
item_counts: [100, 1000, 5000, 10000, 20000, 50000, 100000]

# Test settings
test_size: 1000  # Number of items to test retrieval on
batch_size: 10000  # Batch size for writes/reads

# Reproducibility
seed: 42

# Device
device: "auto"  # "cpu", "cuda", or "auto"
